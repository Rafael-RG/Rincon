<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage
    x:Class="Rincon.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:extensions="clr-namespace:Rincon.Extensions"
    xmlns:pages="clr-namespace:Rincon.Common.Pages;assembly=Rincon.Common"
    xmlns:viewModels="clr-namespace:Rincon.ViewModels"
    x:DataType="viewModels:LoginViewModel"
    xmlns:local="clr-namespace:Rincon.Converters"
    x:TypeArguments="viewModels:LoginViewModel"
    Shell.NavBarIsVisible="False"
    Shell.NavBarHasShadow="False">

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:InvertedBoolConverter x:Key="InvertedBoolConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>



    <Grid RowSpacing="0" ColumnSpacing="0" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Padding="0">
        <Grid.Background>
            <LinearGradientBrush EndPoint="0,1">
                <GradientStop Offset="0.1" Color="#AD6B2A" />
                <GradientStop Offset="1.0" Color="#D3AA84" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Source="logo_login.png" HeightRequest="150" Margin="0,40,0,20" HorizontalOptions="Center" VerticalOptions="Start"/>

        <Grid Grid.Row="1" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Columna izquierda: Operarios -->
            <Frame Grid.Column="0" HasShadow="True" BackgroundColor="#4A744C" BorderColor="#40000000" CornerRadius="24" Margin="20" VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand">
                <StackLayout Orientation="Vertical" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                    <ImageButton Command="{Binding OperatorsCommand}" Source="operator_login_icon.png" HeightRequest="120" WidthRequest="120" BackgroundColor="#4A744C"/>
                    <Label Text="Operarios" HorizontalTextAlignment="Center" FontSize="20" TextColor="White"/>
                </StackLayout>
            </Frame>

            <!-- Columna derecha: Login -->
            <Frame Grid.Column="1" HasShadow="True" BackgroundColor="#D3AA84" BorderColor="#40000000" CornerRadius="24" Margin="20" VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand">
                <StackLayout VerticalOptions="FillAndExpand" Spacing="10" Padding="20,30">
                    <ActivityIndicator
                        VerticalOptions="Center"
                        HorizontalOptions="CenterAndExpand"
                        IsRunning="True"
                        IsVisible="{Binding IsBusy}"/>

                    <StackLayout IsVisible="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}" Spacing="15">
                        <Frame HorizontalOptions="FillAndExpand" VerticalOptions="Start" CornerRadius="18" BackgroundColor="#FAFAFA" BorderColor="Transparent" Padding="0">
                            <Entry
                                BackgroundColor="Transparent"
                                PlaceholderColor="#797979"
                                FontSize="18"
                                AutomationId="userNameEntry"
                                HorizontalOptions="FillAndExpand"
                                MaxLength="50"
                                Placeholder="{extensions:Localize Username}"
                                Text="{Binding Username}"
                                TextColor="#303030"/>
                        </Frame>

                        <Frame CornerRadius="18" BackgroundColor="#FAFAFA" BorderColor="Transparent" HorizontalOptions="FillAndExpand" VerticalOptions="Start" Padding="0">
                            <Entry
                                VerticalOptions="Center"
                                TextColor="#303030"
                                FontSize="18"
                                PlaceholderColor="#797979"
                                HorizontalOptions="FillAndExpand"
                                IsPassword="true"
                                MaxLength="50"
                                Placeholder="{extensions:Localize Password}"
                                Text="{Binding Password}" />
                        </Frame>

                        <Label Text="{extensions:Localize RecoverPassword}" FontSize="16" HorizontalTextAlignment="End" TextColor="#79280F" VerticalOptions="Start" Margin="0,0,0,0">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding RecoveryPasswordCommand}"/>
                            </Label.GestureRecognizers>
                        </Label>

                        <Button
                            VerticalOptions="EndAndExpand"
                            BackgroundColor="#4A744C"
                            Margin="0,10,0,0"
                            TextColor="#FAFAFA"
                            HeightRequest="45"
                            FontSize="18"
                            Command="{Binding LoginCommand}"
                            Text="{extensions:Localize Login}" 
                            CornerRadius="18"/>

                        <Button
                            BackgroundColor="#AD6B2A"
                            Margin="0,10,0,0"
                            HeightRequest="45"
                            TextColor="#FAFAFA"
                            FontSize="18"
                            Command="{Binding CheckinCommand}"
                            Text="{extensions:Localize SignUp}" 
                            CornerRadius="18"/>
                    </StackLayout>
                </StackLayout>
            </Frame>
        </Grid>
    </Grid>

</pages:BaseContentPage>
